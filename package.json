{
  "name": "tuescher-systeme",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "docker-compose up",
    "build": "docker-compose build",
    "start": "docker-compose up -d",
    "stop": "docker-compose down",
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run develop"
  }
}
